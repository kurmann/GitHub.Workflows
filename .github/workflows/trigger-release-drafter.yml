# .github/workflows/trigger-release-drafter.yml im aufrufenden Repository
name: Trigger Release Drafter

on:
  push:

jobs:
  call_release_drafter:
    uses: ./.github/workflows/release-drafter.yml
    secrets:
      github_token: ${{ secrets.GITHUB_TOKEN }}
